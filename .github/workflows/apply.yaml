name: Apply
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Apply - Provsions Infra
        run: bash init.sh apply test hvs.SF62pXEFdIJlbHuyHH43xTAL

      - name: Reading the version
        run: cat /etc/*release
